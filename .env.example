# Supabase — get from your Supabase project settings (API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic — for AI summaries / digests
ANTHROPIC_API_KEY=

# Protects the /api/ingest cron endpoint. Set any random string.
CRON_SECRET=

# Optional: admin passphrase to access /admin (simple gate for MVP)
ADMIN_TOKEN=changeme
